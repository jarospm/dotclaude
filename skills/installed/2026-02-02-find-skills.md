# find-skills

**Installed:** 2026-02-02
**Source:** `vercel-labs/skills`
**Agents:** Claude Code, Codex

Helps discover and install agent skills from the open ecosystem.

## Install

```bash
npx skills add vercel-labs/skills -g -a claude-code -a codex -s find-skills
```

## Notes

- Not in this repo — installed via `npx skills`, lives in `~/.agents/skills/`
- Symlinked into `~/.claude/skills/` and `~/.codex/skills/`
