# web-design-guidelines

**Installed:** 2026-02-15
**Source:** `vercel-labs/agent-skills`
**Agents:** Claude Code, Codex

Audits UI code against 100+ rules covering accessibility, performance, and UX.

## Install

```bash
npx skills add vercel-labs/agent-skills -g -a claude-code -a codex -s web-design-guidelines
```

## Notes

- Not in this repo — installed via `npx skills`, lives in `~/.agents/skills/`
- Symlinked into `~/.claude/skills/` and `~/.codex/skills/`
