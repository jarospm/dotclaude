# vercel-react-best-practices

**Installed:** 2026-02-15
**Source:** `vercel-labs/agent-skills`
**Agents:** Claude Code, Codex

40+ React/Next.js optimization rules across 8 categories — waterfalls, bundle size, SSR, data fetching, re-renders.

## Install

```bash
npx skills add vercel-labs/agent-skills -g -a claude-code -a codex -s vercel-react-best-practices
```

## Notes

- Not in this repo — installed via `npx skills`, lives in `~/.agents/skills/`
- Symlinked into `~/.claude/skills/` and `~/.codex/skills/`
