# playwright-cli

**Installed:** 2026-02-17
**Source:** `microsoft/playwright-cli`
**Agents:** Claude Code, Codex

Automates browser interactions for web testing, form filling, screenshots, and data extraction using Playwright.

## Install

```bash
npx skills add microsoft/playwright-cli -g -a claude-code -a codex -s playwright-cli
```

## Notes

- Not in this repo — installed via `npx skills`, lives in `~/.agents/skills/`
- Symlinked into `~/.claude/skills/` and `~/.codex/skills/`
- See also `tools/icebox/2026-02-11-playwright-cli.md` for the standalone CLI tool reference
