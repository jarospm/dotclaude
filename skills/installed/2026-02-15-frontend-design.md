# frontend-design

**Installed:** 2026-02-15
**Source:** `anthropics/skills`
**Agents:** Claude Code, Codex
**Previously:** Claude Code plugin (`frontend-design@claude-plugins-official`), uninstalled 2026-02-15

Creates distinctive, production-grade frontend interfaces with high design quality. Generates creative, polished code that avoids generic AI aesthetics.

## Install

```bash
npx skills add anthropics/skills -g -a claude-code -a codex -s frontend-design
```

## Notes

- Not in this repo — installed via `npx skills`, lives in `~/.agents/skills/`
- Symlinked into `~/.claude/skills/` and `~/.codex/skills/`
- Same content as the former Claude Code plugin — migrated to `npx skills` for cross-agent support
